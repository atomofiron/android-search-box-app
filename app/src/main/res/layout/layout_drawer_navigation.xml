<?xml version="1.0" encoding="utf-8"?>
<merge
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:parentTag="app.atomofiron.searchboxapp.custom.view.DrawerView"
    >
    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/drawer_rv"
        android:paddingTop="36dp"
        android:clipToPadding="false"
        android:scrollbars="none"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:reverseLayout="true"
        />
    <LinearLayout
        android:id="@+id/drawer_title_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        >
        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/drawer_title"
            android:layout_width="0dp"
            android:layout_height="36dp"
            android:layout_weight="1"
            android:textColor="?attr/colorAccent"
            android:paddingStart="48dp"
            android:gravity="center_vertical"
            android:textStyle="bold"
            tools:text="Title"
            />
        <androidx.appcompat.widget.AppCompatImageButton
            android:id="@+id/drawer_ib_drawer_side"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:src="@drawable/ic_drawer_end"
            android:background="@drawable/ripple_32"
            android:paddingHorizontal="@dimen/padding_semi"
            android:tint="?attr/colorOutlineVariant"
            />
    </LinearLayout>
    <app.atomofiron.searchboxapp.custom.view.InsetsBackgroundView
        android:id="@+id/insets_background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:statusBar="false"
        app:navigationBar="bottom"
        />
</merge>