<?xml version="1.0" encoding="utf-8"?>
<merge
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:parentTag="app.atomofiron.searchboxapp.custom.view.VerticalDockView"
    >
    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/drawer_rv"
        android:paddingTop="36dp"
        android:clipToPadding="false"
        android:overScrollMode="never"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        />
    <LinearLayout
        android:id="@+id/drawer_title_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/colorSurfaceVariant"
        >
        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/drawer_title"
            android:layout_width="0dp"
            android:layout_height="36dp"
            android:layout_weight="1"
            android:textColor="?attr/colorAccent"
            android:paddingStart="44dp"
            android:gravity="center_vertical"
            android:textStyle="bold"
            tools:text="Title"
            />
        <androidx.appcompat.widget.AppCompatImageButton
            android:id="@+id/drawer_ib_dock_side"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:src="@drawable/ic_dock_end"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:paddingEnd="12dp"
            android:tint="?attr/colorContent"
            android:alpha="0.5"
            />
    </LinearLayout>
    <app.atomofiron.searchboxapp.custom.view.SystemBarsBackgroundView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:statusBar="false"
        app:navigationBar="bottom"
        />
</merge>